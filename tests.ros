#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -- $0 "$@"
|#
(progn
  (ros:ensure-asdf))

(let ((root (uiop:pathname-directory-pathname *load-pathname*)))
  (push root asdf:*central-registry*))

(ql:quickload :bknr.ttl/tests :verbose nil)

(defun main (&rest argv)
  "Runs the bknr.ttl test suite and exits 0 on success, 1 on failure."
  (declare (ignorable argv))
  (uiop:quit (if (bknr.ttl/tests:run-tests) 0 1)))
